<UserControl x:Class="_2SemesterOpgave.Pages.FavoritesPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:_2SemesterOpgave.Pages"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
	<UserControl.Resources>

		<DataTemplate x:Key="ArticleTemplate">
			<Border Margin="10"
                    Cursor="Hand"
                    CornerRadius="10"
                    Background="White"
                    BorderBrush="#E6E6E6"
                    BorderThickness="1">

				<Button Click="FavoriteArticlePageButton_Click"
                        Background="Transparent"
                        BorderBrush="Transparent"
                        BorderThickness="0">

					<Grid>

						<Grid.RowDefinitions>
							<RowDefinition Height="250"/>
							<RowDefinition Height="*"/>
						</Grid.RowDefinitions>

						<Image Source="/Assets/Articles/ArticlePlaceholder.jpg"
                               Grid.Row="0"/>

						<Grid Grid.Row="1" Margin="10">

							<Grid.RowDefinitions>
								<RowDefinition Height="Auto"/>
								<RowDefinition Height="Auto"/>
								<RowDefinition Height="Auto"/>
								<RowDefinition Height="Auto"/>
								<RowDefinition Height="Auto"/>
								<RowDefinition Height="Auto"/>
							</Grid.RowDefinitions>

							<TextBlock Text="{Binding Title}"
                                       FontWeight="Bold"
                                       FontSize="14"/>

							<TextBlock Grid.Row="1"
                                       Text="{Binding Description}"
                                       TextWrapping="Wrap"/>

							<TextBlock Grid.Row="2"
                                       Text="{Binding Brand.Name}"
                                       FontWeight="Bold"
                                       FontSize="12"/>

							<TextBlock Grid.Row="3"
                                       Text="{Binding Size.Name}"
                                       FontWeight="Bold"
                                       FontSize="12"/>

							<TextBlock Grid.Row="4"
                                       Text="{Binding Category.Name}"
                                       FontWeight="Bold"
                                       FontSize="12"/>

							<StackPanel Grid.Row="5"
                                        Orientation="Horizontal">

								<TextBlock Text="Dagspris: "
                                           FontWeight="Bold"/>

								<TextBlock Text="{Binding DailyPrice, StringFormat={}{0:F2} kr, ConverterCulture=da-DK}"/>

							</StackPanel>

						</Grid>

					</Grid>

				</Button>

			</Border>
		</DataTemplate>

	</UserControl.Resources>

	<ScrollViewer>

		<Grid Margin="50">

			<Grid.RowDefinitions>
				<RowDefinition Height="*" />
			</Grid.RowDefinitions>

			<Grid>

				<Grid.RowDefinitions>
					<RowDefinition Height="100"/>
					<RowDefinition Height="*"/>
				</Grid.RowDefinitions>

				<TextBlock Grid.Row="0"
                           Text="Mine Favoritter"
                           FontSize="50"/>

				<ItemsControl x:Name="FavoriteArticlesItemsControl"
                              Grid.Row="1"
                              ItemTemplate="{StaticResource ArticleTemplate}">

					<ItemsControl.ItemsPanel>
						<ItemsPanelTemplate>
							<WrapPanel Orientation="Horizontal"/>
						</ItemsPanelTemplate>
					</ItemsControl.ItemsPanel>

				</ItemsControl>

			</Grid>

		</Grid>

	</ScrollViewer>
</UserControl>
